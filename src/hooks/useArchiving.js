const hook = ()=>{
    const headerData = [
        {name: 'First name', field: 'firstname'},
        {name: 'Last name', field: 'lastname'}
    ]

    return {
        headerData
    }
}

export default hook